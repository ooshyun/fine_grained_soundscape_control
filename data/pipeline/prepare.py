from __future__ import annotations
from pathlib import Path


def run_prepare(curated_dir: Path, raw_dir: Path, output_dir: Path, ontology, data_dir: Path) -> None:
    """Prepare final training data from curated CSVs."""
    raise NotImplementedError("prepare stage not yet implemented")
