from __future__ import annotations
from pathlib import Path


def run_collect(sources, raw_dir: Path, curated_dir: Path) -> None:
    """Collect and curate datasets into train/val/test CSVs."""
    raise NotImplementedError("collect stage not yet implemented")
